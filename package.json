{
  "name": "@discord-plays-pokemon/monorepo",
  "version": "1.0.0",
  "scripts": {
    "start": "npx tsx packages/backend/src/index.ts",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "prettier": "prettier .",
    "prettier:fix": "prettier -w ."
  },
  "workspaces": [
    "packages/common",
    "packages/backend",
    "packages/frontend"
  ],
  "devDependencies": {
    "prettier": "^3.0.3",
    "prettier-plugin-tailwindcss": "^0.5.4"
  },
  "engineStrict": true
}
